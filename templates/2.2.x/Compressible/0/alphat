/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  2.1.0                                 |
|   \\  /    A nd           | Web:      www.OpenFOAM.org                      |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       volScalarField;
    location    "0";
    object      alphat;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

#include "${FOAM_CASE}/constant/boundaryConditions/basic";

dimensions      [ 1 -1 -1 0 0 0 0 ];

internalField   uniform 0;

boundaryField
{
#   include "${WM_PROJECT_DIR}/etc/caseDicts/setConstraintTypes";

    defaultGroup { $:bc.default.alphat }

    inletGroup { $:bc.fixedInlet.alphat; }

    wallGroup { $:bc.walls.alphat; }

    slipGroup       { $:bc.slipWalls.alphat; }

    movingWallGroup { $:bc.walls.alphat; }

    ambienceGroup { $:bc.totalPressure.alphat; }

    outflowGroup { $:bc.pressureOutlet.alphat; }

    farfieldGroup { $:bc.freestream.alphat; }

#   include "include/general.in";
}

// ************************************************************************* //
